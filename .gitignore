# ----------------------------------------------------------------------
# 1. 操作系统生成的垃圾文件 (System Files)
# ----------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# ----------------------------------------------------------------------
# 2. 依赖库 (Dependencies)
# ----------------------------------------------------------------------
node_modules/
.pnp
.pnp.js

# ----------------------------------------------------------------------
# 3. 构建产出 (Build Outputs)
# ----------------------------------------------------------------------
# Vite 默认构建输出目录
dist/
dist-ssr/

# TypeScript 编译缓存或输出 (如果有单独配置 tsc)
*.tsbuildinfo

# ----------------------------------------------------------------------
# 4. 环境变量与密钥 (Environment Variables)
# ----------------------------------------------------------------------
# 务必忽略，防止 API Key 或后端配置泄露
.env
.env.local
.env.*.local
# 但保留 .env.example 作为模板提交
!*.example

# ----------------------------------------------------------------------
# 5. IDE 与编辑器配置 (Editor Configs)
# ----------------------------------------------------------------------
# VS Code (根据团队习惯，有时保留 settings.json，这里默认忽略用户级配置)
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
*.code-workspace

# JetBrains (WebStorm, IntelliJ)
.idea/
*.iml

# ----------------------------------------------------------------------
# 6. 日志文件 (Logs)
# ----------------------------------------------------------------------
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ----------------------------------------------------------------------
# 7. 游戏开发专用忽略项 (Game Dev Specific)
# ----------------------------------------------------------------------
# 忽略二进制存档/快照文件 (src/data/serialization.ts 生成的测试文件)
*.save
*.sav
*.bin
*.snapshot

# 忽略测试生成的录像文件 (tools/replay-inspector/ 产生的临时数据)
*.replay
*.rep

# 忽略美术源文件 (通常体积巨大，不建议放入 Git，除非使用 Git LFS)
# 建议只提交压缩后的 .png/.webp/.json
*.psd
*.ai
*.raw
# 如果你的 assets 目录包含未处理的高保真音频
*.wav
!src/adapters/render/assets/*.wav

# ----------------------------------------------------------------------
# 8. 工具链生成的临时文件 (Tools & Cache)
# ----------------------------------------------------------------------
# 你的工具目录产生的临时缓存
src/tools/cache/
coverage/
.nyc_output/

# 虚拟文件系统缓存 (如果有本地调试生成的)
*.vfs
